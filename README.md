# dashplayrs

A pure Rust implementation of an MPEG-DASH player.

## Development

Requires Rust stable.

Build:

```bash
cargo build
```

Test:

```bash
cargo test
```

Format and lint:

```bash
cargo fmt --all
cargo clippy --all-targets -- -D warnings
```
